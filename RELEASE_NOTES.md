Bản này sửa hai lỗi khiến thông báo cập nhật không đến tay bạn, và đổi cách nó xuất hiện:

- **Thông báo cập nhật giờ hiện thành hộp thoại** kèm danh sách thay đổi (chính là những dòng bạn đang đọc), với hai nút Cập nhật / Để sau. Chọn "Để sau" thì lời mời thu về mũi tên nhỏ dưới chân cửa sổ — bấm vào đó khi nào muốn.
- Máy khởi động xong mà mạng lên chậm thì ứng dụng **tự hỏi lại** thay vì im lặng bỏ qua; đang mở sẵn cửa sổ cũng nhận được tin ngay.
- Cuộn nhanh trong chế độ lưới không còn để lại các ô ảnh trống vĩnh viễn.
- Xem trước video: bấm dừng và tua bằng chuột đã hoạt động; phím Space tạm dừng video thay vì đóng cửa sổ.
- Danh sách kết quả nhớ các lựa chọn của bạn (lưới, sắp xếp, loại tệp) giữa các lần mở; thêm phím Home/End/Ctrl+A; chế độ trùng lặp điều khiển được bằng bàn phím.
