> ⚠️ **Bản thử.** Chỉ dành cho máy thử nghiệm. Các máy đang dùng v1.0.4 sẽ không nhận được thông báo cập nhật lên bản này.

**Lọc kết quả theo ổ đĩa.** Tìm ra hàng trăm kết quả nằm rải trên nhiều ổ, giờ có một hàng nút ngay trên danh sách để thu về đúng ổ mình cần:

- Hàng nút hiện số kết quả của từng ổ — `Tất cả 248` · `D: 31` · `Y: 217`. Bấm một nút là danh sách chỉ còn ổ đó, bấm lại là bỏ lọc.
- **Ổ mạng mang màu cam** và luôn đứng cuối hàng, để phân biệt ngay với ổ trong máy.
- Mỗi dòng kết quả có thêm nhãn ổ nhỏ, nên nhìn là biết tệp nằm đâu mà không phải đọc hết đường dẫn.
- Lọc diễn ra **tức thì** — không phải tìm lại, không phải chờ NAS.
- Hàng nút chỉ hiện khi kết quả nằm trên nhiều ổ. Cùng một ổ thì không có gì thay đổi.

Bộ lọc không giữ lại giữa các lần mở ứng dụng, và tự bỏ khi lần tìm mới không còn kết quả nào ở ổ đang chọn — để không bao giờ có chuyện màn hình trống trong khi tệp vẫn nằm đó, chỉ ở ổ khác.
